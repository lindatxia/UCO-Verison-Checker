# Nothing here to see yet, but here's an example!
# http://flask.pocoo.org/docs/0.12/tutorial/schema/#tutorial-schema 

-- drop table if exists entries;
-- create table entries (
--   id integer primary key autoincrement,
--   title text not null,
--   'text' text not null
-- );